
services:
  - type: web
    name: smart-energy-security-api
    env: node
    buildCommand: npm run build
    startCommand: node index.js
    envVars:
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production